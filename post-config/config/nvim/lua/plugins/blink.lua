return {
  {
    "saghen/blink.cmp",
    optional = true,
    opts = {
      completion = {
        list = {
          selection = {
            preselect = false, -- This stops preselecting the first item
            -- auto_insert = false,  -- Optional: prevent auto-insert on typing
          },
        },
      },
    },
  },
}
